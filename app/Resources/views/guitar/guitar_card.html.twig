{{ include('guitar/guitar_view.html.twig', { 'guitar': guitar }) }}
<div class="col-md-3">
    <div class = "thumbnail" data-toggle="modal" href=#{{ guitar.id }} >
        <img src = '{{ asset('images/') }}{{guitar.imgPath}}' alt = {{ guitar.name }}>
        <div class = "caption">
            <h3>{{ guitar.name }}</h3>
            <p>{{ guitar.description }}</p>    
        </div>
    </div>
</div>

